version: '3.8'

services:
  nextjs-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      # Persist SQLite DB on the host (project-local directory)
      - ./data:/app/data
    restart: always # Always restart the container if it stops
